import { describe, test } from "vitest";

describe("Organizer self-service onboarding flow", () => {
  test.todo(
    "accepts an invitation and creates a competition with initial edition",
  );
  test.todo(
    "persists scoreboard theme defaults and assigns competition_admin role",
  );
  test.todo(
    "exposes created entities via typed services for dashboard hydration",
  );
});
