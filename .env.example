# Database connection string (PostgreSQL 17 container from compose.yaml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tournament_app"

# better-auth secrets and email identity
BETTER_AUTH_SECRET="change-me"
BETTER_AUTH_EMAIL_SENDER="no-reply@example.com"

# Base application URL surfaced to the browser (Norwegian Bokmål UI)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Structured logging level consumed by pino
PINO_LOG_LEVEL="info"
